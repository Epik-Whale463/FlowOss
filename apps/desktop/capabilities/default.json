{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay and settings windows: core events + window control",
  "windows": ["overlay", "settings"],
  "permissions": ["core:default"]
}
